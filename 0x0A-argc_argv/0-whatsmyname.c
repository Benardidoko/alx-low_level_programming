#include <stdio.h>
/**
 * main - the function prints its own name
 * @argc: the argument do count
 * @argv: the array pointing to the arguments
 * Return: 0 if successful
 */
int main(int argc, char **argv)
{
	argc = 0;

	printf("%s\n", argv[argc]);
	return (0);
}
